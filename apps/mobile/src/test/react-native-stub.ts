// Stub module for react-native in Vitest.
// Vite/Rollup cannot parse React Native's Flow syntax, so this stub
// prevents Vite from loading the real module. The actual mock
// implementations are provided by vi.mock('react-native', ...) in setup.ts.
export {};
